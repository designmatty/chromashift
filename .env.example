# Optional Windows Authenticode signing credentials consumed by electron-builder.
# Store real values in the environment or CI secrets, never in this file.
WIN_CSC_LINK=C:\path\to\certificate.pfx
WIN_CSC_KEY_PASSWORD=replace-with-secret
